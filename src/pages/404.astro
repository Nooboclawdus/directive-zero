---
import BaseLayout from '../layouts/BaseLayout.astro';
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<BaseLayout title="Signal Perdu" description="Page introuvable — fréquence non répertoriée.">
  <div class="min-h-[70vh] flex items-center justify-center px-4">
    <div class="text-center max-w-lg">
      <!-- Glitch 404 -->
      <div class="relative mb-8">
        <h1 class="font-[var(--font-heading)] text-[8rem] sm:text-[10rem] font-black leading-none text-[var(--color-crimson)] opacity-20 select-none">
          404
        </h1>
        <div class="absolute inset-0 flex items-center justify-center">
          <span class="font-[var(--font-heading)] text-4xl sm:text-5xl font-bold tracking-widest text-[var(--color-crimson)] glitch-text">
            SIGNAL PERDU
          </span>
        </div>
      </div>

      <!-- Terminal block -->
      <div class="bg-[var(--color-bg-tertiary)] border border-[var(--color-border)] rounded-lg p-6 mb-8 text-left font-[var(--font-mono)] text-sm">
        <div class="text-[var(--color-text-muted)] mb-2">
          <span class="text-[var(--color-cyan)]">BIE://</span>réseau-conciliaire<span class="text-[var(--color-crimson)]">&gt;</span> localiser fréquence
        </div>
        <div class="text-[var(--color-crimson)] mb-2">
          ⚠ ERREUR : Fréquence non répertoriée dans les registres du Concile
        </div>
        <div class="text-[var(--color-text-muted)] mb-2">
          <span class="text-[var(--color-cyan)]">BIE://</span>réseau-conciliaire<span class="text-[var(--color-crimson)]">&gt;</span> diagnostic
        </div>
        <div class="text-[var(--color-text-secondary)]">
          Hypothèses : effacement mémoriel, brouillage xéno, ou anomalie de Flux.
          <br />
          Recommandation : retour au point d'extraction.
        </div>
      </div>

      <!-- Actions -->
      <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
        <a href={`${base}/`}
          class="inline-flex items-center gap-2 px-6 py-3 bg-[var(--color-crimson)] text-white font-[var(--font-heading)] text-sm tracking-wider uppercase rounded hover:bg-[var(--color-crimson-dark)] transition-colors">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
          </svg>
          Point d'extraction
        </a>
        <a href={`${base}/glossaire`}
          class="inline-flex items-center gap-2 px-6 py-3 border border-[var(--color-border)] text-[var(--color-text-secondary)] font-[var(--font-heading)] text-sm tracking-wider uppercase rounded hover:border-[var(--color-cyan)] hover:text-[var(--color-cyan)] transition-colors">
          Glossaire du Concile
        </a>
      </div>

      <!-- Flavor text -->
      <p class="mt-12 text-[var(--color-text-muted)] text-xs italic">
        « Toute information disparue n'est pas perdue. Certaines ont été effacées volontairement. »
        <br />
        <span class="text-[var(--color-text-muted)]/50">— Archiviste Kel Vaarn, Rapport C-7781</span>
      </p>
    </div>
  </div>

  <style>
    .glitch-text {
      animation: glitch 3s infinite;
    }
    @keyframes glitch {
      0%, 90%, 100% { opacity: 1; transform: translate(0); }
      92% { opacity: 0.8; transform: translate(-2px, 1px); }
      94% { opacity: 0.9; transform: translate(2px, -1px); }
      96% { opacity: 0.7; transform: translate(-1px, 2px); }
      98% { opacity: 1; transform: translate(1px, -1px); }
    }
  </style>
</BaseLayout>
